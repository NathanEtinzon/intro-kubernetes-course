# Pré‑requis

- Le Deployment `web-demo` du Lab 1

- Si le Deployment `web-demo` a été supprimé lors du nettoyage du Lab 1, rejouer l’étape 1 du Lab 1 avant de commencer ce lab.

**Vérifier :**

```bash
kubectl get deploy web-demo
kubectl get pods -l app=web
```
